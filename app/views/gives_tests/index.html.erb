<p id="notice"><%= notice %></p>

<h1>Gives Tests</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gives_tests.each do |gives_test| %>
      <tr>
        <td><%= link_to 'Show', gives_test %></td>
        <td><%= link_to 'Edit', edit_gives_test_path(gives_test) %></td>
        <td><%= link_to 'Destroy', gives_test, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Gives Test', new_gives_test_path %>
