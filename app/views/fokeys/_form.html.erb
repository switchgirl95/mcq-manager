<%= form_with(model: fokey, local: true) do |form| %>
  <% if fokey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fokey.errors.count, "error") %> prohibited this fokey from being saved:</h2>

      <ul>
      <% fokey.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
